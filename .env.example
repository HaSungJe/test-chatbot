GOOGLE_API_KEY=AIza...

DB_HOST=localhost
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=ragdb

# dotenv-expand이 위 변수들을 참조해서 자동 조합
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
